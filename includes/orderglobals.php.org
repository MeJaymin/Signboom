<?
	global $HTTP_POST_VARS;
	global $database_DBConn;
	global $DBConn;

	global $ProductDesc;
	global $oproduct;
	global $olam;
	global $obacklight;
	global $ocutting;

	global $setup_printing;
	global $setup_lamination;
	global $setup_cutting;
	global $lineal_printing;
	global $lineal_backlighting;
	global $lineal_lamination;
	global $lineal_cutting;

	global $total_setup;
	global $total_lineal;
	global $waste;
	global $printable_width;

	global $calculated_footage;
	global $total_cost;
	global $unit_cost;
	global $ready_ship;

	global $product;
	global $item;
	global $itemList;
	global $laminate;
	global $backlight;
	global $cutting;

	global $tiling;
	global $tilecount;
	global $horizontal;
	global $vertical;
	global $rotated;
	global $images;

	global $quantity;
	global $outputwidth;
	global $outputheight;

	global $errMsg;
	global $productIDX;
	global $laminateIDX;
	global $backlightIDX;
	global $cuttingIDX;
	
?>